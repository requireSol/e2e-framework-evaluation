{
  "name": "@e2e-framework-evaluation/e2e-cypress",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "cy:open": "cypress open --browser chrome --env baseUrl=http://localhost:3000",
    "cy:run": "cypress run --browser chrome --env baseUrl=http://localhost:3000",
    "cy:install": "cypress install"
  },
  "private": true,
  "dependencies": {
    "@e2e-framework-evaluation/lib-common": "workspace:*",
    "authenticator": "^1.1.5",
    "cypress": "^13.16.1"
  },
  "devDependencies": {
    "@types/authenticator": "^1.1.4"
  }
}
